import styled from 'styled-components/macro';

export default function Loading() {
  return (
    <Container>
      Loading...
    </Container>
  );
}

const Container = styled.p`
  text-align:center;
`;
