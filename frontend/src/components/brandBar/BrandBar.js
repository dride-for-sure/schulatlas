import styled from 'styled-components/macro';

const BrandBar = styled.hr`
  background: var(--gradient-primary);
  border: 0;
  margin: 0;
  padding: 0;
  height: 4px;
  width: 100%;
`;

export default BrandBar;
