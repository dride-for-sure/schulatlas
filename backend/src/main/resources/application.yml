spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  data:
    mongodb:
      database: schulatlas
aws:
  region: eu-central-1
  access:
    key:
      id: top-secret
      secret: top-secret
  s3:
    audio:
      bucket: schulatlas
google:
  api:
    geo: top-secret
security:
  jwt:
    secret: top-secret
server:
  tomcat:
    max-swallow-size: -1